#include "../include/memory_pool/common.hpp"
#include <cstdlib>
#include <cstring>

namespace memory_pool {

// This file contains implementations of common utility functions
// that are used throughout the memory pool library.

// Currently, most of the common utilities are inline functions
// defined in the header file, so this file is relatively small.
// As the library grows, more implementations can be added here.

} // namespace memory_pool